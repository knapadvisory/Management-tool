# TeamHub — Android app

The Android app is the TeamHub web app wrapped in a native shell with
[Capacitor](https://capacitorjs.com/). It loads the live site
(`https://teamhub.knapadvisory.com`) inside a native WebView, so it always runs
the current version — no app-store update needed when the web app changes — and
adds native camera/mic access for calls, a splash screen, and the plumbing for
push notifications.

- **App name:** TeamHub
- **App ID:** `com.knapadvisory.teamhub`
- **Project:** `client/android/` (generated by Capacitor; committed)
- **Config:** `client/capacitor.config.json`

## Get an installable APK (no local Android setup)

A GitHub Actions workflow builds a signed-for-debug APK you can sideload:

1. Open the repo's **Actions** tab → **Build Android APK** → **Run workflow**
   (it also runs automatically when `client/**` changes).
2. When it finishes, download the **`teamhub-debug-apk`** artifact.
3. Copy `app-debug.apk` to an Android phone and open it (allow "install from
   unknown sources"). Done.

The debug APK is for internal distribution/testing. For the Play Store you'll
build a **release** bundle (`./gradlew bundleRelease`) signed with your own
keystore — see "Play Store" below.

## Build locally

Prerequisites: Node 20+, JDK 17, Android Studio (or the Android SDK + CLI).

```bash
cd client
npm ci
npm run build          # build the web app
npx cap sync android   # copy web + native deps into android/
npx cap open android   # opens Android Studio → Run ▶ to a device/emulator
```

Or from the command line without Android Studio:

```bash
cd client/android
./gradlew assembleDebug
# APK at client/android/app/build/outputs/apk/debug/app-debug.apk
```

## How it's wired

- **Loads the live server.** `capacitor.config.json` sets
  `server.url = https://teamhub.knapadvisory.com`. To point the app at a
  different environment (staging, a LAN IP for testing), change that value and
  re-run `npx cap sync android`.
- **Calls work in the app.** `AndroidManifest.xml` declares `CAMERA`,
  `RECORD_AUDIO`, and `MODIFY_AUDIO_SETTINGS`; `MainActivity.java` requests them
  at launch so WebRTC `getUserMedia` succeeds the first time someone starts an
  audio/video call. (Group calls still need a TURN server configured on the
  backend to connect across mobile networks — see the server deploy config.)
- **Splash screen** uses the TeamHub navy (`#1a1d29`).

## Next steps (not done yet)

- **Push notifications.** The `@capacitor/push-notifications` plugin is
  installed and permission is requested, but delivering pushes needs: (1) a
  Firebase project + `google-services.json` dropped into `client/android/app/`,
  (2) client code that registers the device and sends its FCM token to the
  backend, and (3) a backend path that sends FCM messages on new
  messages/calls. This is the natural follow-up to make the app useful in the
  background.
- **App icon.** Currently the default Capacitor launcher icon. Drop a
  1024×1024 PNG and run `npx @capacitor/assets generate` to produce all sizes.
- **Play Store release.** Generate an upload keystore, configure signing in
  `android/app/build.gradle`, build `./gradlew bundleRelease`, and upload the
  `.aab` to the Play Console.
